name: Docker Build
on: [push, pull_request]

jobs:
  push_to_registries:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Create tagname
        id: create_tagname
        run: |
          REF=${GITHUB_HEAD_REF:-${GITHUB_REF}}
          NAME_=${REF#refs/heads/}
          NAME=${NAME_#refs/tags/}
          if [ "$NAME" = "master" ]; then
            echo "##[set-output name=tagname;]$(echo latest)"
          else
            echo "##[set-output name=tagname;]$(echo ${NAME})"
          fi
      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
        if: "! contains(steps.create_tagname.outputs.tagname, '/')"
      - name: Push to Docker Hub
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: srzzumix/googletest:${{ steps.create_tagname.outputs.tagname }}
      - name: Push to Docker Hub (alpine)
        uses: docker/build-push-action@v2
        with:
          context: alpine
          push: true
          tags: srzzumix/googletest:${{ steps.create_tagname.outputs.tagname }}-alpine
        if: "! contains(steps.create_tagname.outputs.tagname, '/')"
